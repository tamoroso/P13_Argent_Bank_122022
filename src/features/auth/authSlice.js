import { createSlice } from "@reduxjs/toolkit";

export const authSlice = createSlice({
  name: "auth",
  initialState: {},
  reducers: {},
});

// TODO:  Export reducers from authSlice.actions

export default authSlice.reducer;
